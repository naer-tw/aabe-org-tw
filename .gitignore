# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Office lock files
.~lock.*

# Local zip / archive
*.zip
*.tar.gz

# Editor
.vscode/
.idea/
*.swp
*~

# Node (in case we add tooling later)
node_modules/
package-lock.json

# Build artifacts
dist/
.cache/

# Pagefind 搜尋索引（部署前由 build_search_index.sh 產生，不進 repo）

# 政策站搜尋索引暫存標記目錄（build_search_index.sh 建置過程用，不進 repo；
# public/pagefind-policy/ 是最終索引檔，那個要進 repo，跟 public/pagefind/ 同待遇）
.policy-search-staging/

# Python
__pycache__/
*.pyc
.pytest_cache/

# 內部審查／複驗／規劃筆記不進公開 repo（真源另存工作區與看板）
_source/審查/
