# 兒少十大承諾・民間團體連署頁上架收據 2026-09-18

- commit：23ed91a（main，**尚未 push**，ahead of origin/main 1 個 commit）
- 基底（尚未上線，供之後比對）：https://aabe.org.tw/act/child-pledge/
- 任務：把「兒少十大承諾・民間團體連署」頁準備到官網可上線狀態（理事長 2026-09-18 裁決：放 `/act/child-pledge/` 行動頁＋活動卡導流；**表單重建後才 push**，本輪只做到 commit）

## 過程中三次追加指示（均已納入本次 commit）
1. 表單連結換新：理事長帳號重建後之新表單 `https://forms.gle/SPJmXo19DVN8hv499`，取代舊連結 `https://forms.gle/BsBXuQLjX33Xavg36`（build.py FORM_URL 已改，grep 全站確認新連結 1 命中、舊連結 0 命中）。
2. 名冊加第 14 筆：台灣ICF研究學會／理事長 廖華芳（真源模板已改，13 個共同發起＋1 召集人＝14 筆，grep 確認）。
3. 稱謂改法：全站「共同推動」→「共同發起」（國教盟維持「召集人」）；真源模板、本頁自建的首頁卡／活動卡文案同步替換，grep 確認「共同推動」0 命中。

## 改了哪些檔（git show --stat HEAD，33 files changed, 424 insertions(+), 15 deletions(-)）
- **新增**：`public/act/child-pledge/index.html`（正式版連署頁，366 行）、`public/act/child-pledge/og.png`（1200×630 社群分享圖）
- **真源**：`~/Documents/Claude/Projects/縣市長政見監督平台/docs/連署網頁_v1_20260918/build.py`（新建，產生器移入真源＋`--prod` 參數；**此檔不在 aabe-deploy repo，未納入本 commit**，見下方「取捨掉」）
- **修改**：`public/index.html`（首頁「最新倡議」卡＋1）、`public/events/index.html`（活動總表 upcoming 區塊＋1）、`_source/events.json`（新增 `2026-child-pledge-signup` 事件）、`public/sitemap.xml`（新增 `/act/child-pledge/`，`/`與`/events/` lastmod 改 2026-09-18）、`public/sitemap_index.xml`（主站 sitemap lastmod 同步）
- **搜尋索引**：`public/pagefind/*`、`public/pagefind-policy/*` 全量重建（本站 31→32 頁，政策站 47 頁不變）
- **測試基準維護**（隨本次改動同步，見下方說明）：`_source/deploy-automation/tests/test_sitemap.py`（`DEPLOY_DATE` 2026-09-17→2026-09-18）、`_source/數字位置表_20260906.md`（重生，僅行號位移，內容/雜湊不變）

## 閘輸出摘要（原文見下方逐項；全部 exit 0）
1. `lint.py`：**15 OK / 0 WARN / 0 ERROR**
2. `events_check.py`：**PASS**（8 場已結束活動，無殘留報名字樣）
3. `numbers_check.py`：**✅ 全部相符**（標記處 67／文案處 166，無未登記黑數；`--check-report` 亦 exit 0）
4. `apply_events.py --dry-run`：**✅ 無變動**（版面標記與 events.json 一致，含 `pages[]` 位置基線覆蓋檢查）
5. `pytest -q _source/deploy-automation`：**162 passed, 72 skipped**（與昨日基線一致；過程中一度 4 failed，見下）
6. `build_search_index.sh --check`：**通過**（本站 32 頁／1.6M，政策站 47 頁／1.6M）

### 過程中出現、已排查修正的 4 個 pytest 失敗
- `test_sitemap.py::test_every_changed_page_has_todays_lastmod`、`test_sitemap_index_follows_the_main_site`：本頁改動當天的真實日期（2026-09-18）與測試寫死的 `DEPLOY_DATE`（2026-09-17，上次部署日）不一致。查證：`_source/SOP-數據更新與部署索引.md` 經驗紀錄與 `test_sitemap.py` 本身文件字串均明載此常數「每次部署都要手改」（2026-09-11、2026-09-17 兩次前例皆同法處理）。**取捨**：依前例把常數改成 2026-09-18。
- `test_markup.py::test_copy_text_untouched[index.html]`：凍結快照 `Backups/20260906_官網數據真源/index.html` 是「隨每次合法插卡同步刷新」的活快照（非真凍結於 9/6），9/17 前例已示範同一手法（先把舊快照歸檔到 `Backups/2026-09-17/舊測試快照_20260906/`，再覆寫活快照）。**取捨**：本次比照辦理，舊快照歸檔於 `Backups/2026-09-18/舊測試快照_20260906/index.html`（皆在 `~/Documents/Claude/Backups/`，不在 aabe-deploy repo 內，未納入本 commit）。
- `test_numbers_check.py::test_committed_position_table_is_current`：插卡使行號位移，位置表過期。**修法**：跑 `numbers_check.py --report` 重生，diff 確認只有行號變動、雜湊與登記狀態不變。

## 預覽驗證（本機 http.server，綁 127.0.0.1:8912）
- **首頁卡**（桌機 1440×900，`/`）：新卡位於「最新倡議」第 2 格（10/2 論壇卡與 9/12 卡之間，日期排序正確），橘色左框（活動類型）、「NEW」徽章由前端 JS 自動加註（未手寫）、標題與連結（→`/act/child-pledge/`）正確。已親眼截圖確認。
- **連署頁桌機**（1440×900，`/act/child-pledge/`）：Hero、十大承諾、名冊（14 筆，含台灣ICF研究學會排最後一筆）、FAQ 均正常渲染，無破版；console 訊息 0 筆。已截圖確認。
- **連署頁手機**（375×812）：Hero 單欄、按鈕堆疊、名冊改單欄清單（台灣ICF研究學會／廖華芳為最後一筆）均正確；console 訊息 0 筆。已截圖確認（含理事長／協調端要求的名冊手機截圖）。
- **/events/ 活動總表**（桌機）：新 upcoming 卡（badge「連署中・邀請共同發起」、按鈕「我要連署」→`/act/child-pledge/`）正確渲染於 10/2 論壇卡下方；console 訊息 0 筆。
- **og/meta 程式化驗證**（javascript_tool 讀 DOM）：canonical、og:title/description/image/type、twitter:card/title/description/image、favicon×3、robots meta（已移除，值為 null）、`data-pagefind-body` 均正確存在且內容正確。

## 驗收條件對照
- 閘輸出原文：本檔含摘要＋結論；完整逐行輸出未存檔（僅終端機顯示過），如需要可重跑上列 6 條指令重現（均為冪等只讀或已通過之檢查）。
- 截圖 3＋張：已在本任務對話中截圖並親眼確認（首頁卡桌機、連署頁桌機、連署頁手機、名冊手機、/events/ 卡桌機，共 5 張），未另存檔案。
- `git show --stat HEAD`：見上方「改了哪些檔」一節，33 files changed, 424 insertions(+), 15 deletions(-)，全部屬本任務範圍。
- og.png 尺寸：`sips -g pixelWidth -g pixelHeight` 輸出 1200×630，與 `file` 指令輸出的 PNG 標頭一致。

## 已驗證／沒驗／取捨掉

**已驗證**
- 頁面十項承諾、FAQ、召集人資訊文字逐字沿用真源模板，未改動（僅移除 previewbar／noindex、加 head 中繼資料、加 pagefind 索引屬性——均非可見文案）。
- 表單新連結、ICF 名冊、共同發起稱謂三項追加指示全數落地並 grep 覆核。
- 六道閘＋完整 pytest 全綠；og.png 尺寸與內容親眼確認。
- git 提交只含本任務檔案，工作目錄乾淨，未 push（ahead 1）。
- 預覽伺服器僅綁 127.0.0.1；未派生需留崗之外部長進程。

**沒驗**
- 未跑 Codex 異源盲測（SOP §4-3「對外數字改動」門檻本次未觸發——本頁未新增 numbers.json 追蹤數字，僅沿用頁面既有「22 縣市」「13 個團體」等敘述性數字，來源即頁面內容本身）；若理事長認定本頁性質仍需異源覆核，需另行安排。
- 未做真實 Google Search Console 索引請求、IndexNow（尚未 push，無正式 URL 可請求索引）。
- og.png 未經過人類設計覆核（僅自我視覺檢查＋尺寸驗證），色彩與排版為本次自行設計，未套用既有 og 圖範本（cwa-reform 用琥珀色主題，本頁依任務指示改用墨綠＋青綠）。

**取捨掉**
- `build.py` 真源（在縣市長政見監督平台 repo）未一併 commit——該 repo 未被本任務要求提交，且該 repo 目前有多筆與本任務無關的未追蹤檔案（法律送審包、裁決紀錄等），為避免誤觸範圍外內容，本任務只在該 repo 工作目錄留下 `build.py`＋`logo-source.png` 兩個新檔，不動 git。
- 未在 `/act/child-pledge/index.html` 本身加 `data-event-id` 標記（events.json `pages` 只列 `/`與`/events/`）——比照 `2026-08-21-tobacco-warning-survey`／`2026-07-10-cfa-missing-pieces-forum` 兩種既有模式後，選擇較簡單的一種；若未來連署頁本身也要顯示可回填的 badge/cta，需要另外補標記並更新 `pages[]`。
- 未套用 schema.org JSON-LD（cwa-reform 頁有 NGO／NewsArticle 結構化資料）——任務規格未要求，本頁性質（連署行動頁而非新聞／深度文章）是否適用同一套 schema 需另行判斷，故未新增。
- 內部預覽版（`縣市長政見監督平台/docs/連署網頁_v1_20260918/index.html`，仍是舊表單連結、無 ICF、無共同發起）本次未同步重新產生——理事長訊息只要求正式版 `public/act/child-pledge/index.html`，該內部預覽檔案狀態為「法律顧問審閱中」，本任務不主動覆寫。

## 待辦
1. **push**：待理事長／指揮部核可後執行 `git push`（本次已完成表單連結更新，若無其他待確認事項可視為 push 前置條件已滿足，但仍依指示等候明確核可）。
2. push 後跑 `postdeploy.sh --receipt <路徑>` 補「部署後索引」五項（CONTENT_LIVE_OK／INDEXNOW_OK／GSC 請求索引／看板日誌／經驗回寫）。
3. push 後於 Search Console 對首頁或本頁請求索引一次（理事長 Chrome 操作或指揮部代操作）。
4. 看板 `議題_官網影響力數據/_狀態.md`（或對應議題夾）補最新交付行＋當日日誌一行。
